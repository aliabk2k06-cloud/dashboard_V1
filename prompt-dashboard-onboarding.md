# برومبت البناء: الصفحة الرئيسية + صفحة إضافة تاجر
## نظام "الشرطي الآلي لجمع الوثائق" - داشبورد المحاسب

---

## السياق (Context)

أنت تبني داشبورد لمكتب محاسبة جزائري. النظام يساعد المحاسب على متابعة التجار (عملاء المكتب) وحالة الوثائق الشهرية اللي يجب عليهم إرسالها (فواتير، كشوفات بنكية...).

**المرجع المعماري (Architecture Reference):**
استنسخ هذا المشروع وادرس بنيته جيداً قبل البدء:
```
git clone https://github.com/satnaing/shadcn-admin.git
```
خذ منه فقط:
- بنية الملفات والمجلدات (project structure)
- طريقة تركيب المكونات (component composition patterns)
- إعداد TanStack Router
- إعداد TanStack Table (Data Table component)
- إعداد shadcn/ui + TailwindCSS + Radix UI
- نظام Light/Dark mode

**لا تنسخ:**
- الألوان والهوية البصرية الافتراضية (سنعطيك هوية جديدة بالكامل)
- صفحات المحتوى (Users, Tasks...) - سنبنيها من الصفر بمحتوى مختلف تماماً
- أي نص أو محتوى بالإنجليزية غير ضروري

---

## الستاك التقني المطلوب

- **Frontend:** React + Vite + TypeScript
- **UI:** shadcn/ui (TailwindCSS + Radix UI)
- **Routing:** TanStack Router
- **Tables:** TanStack Table
- **Backend/DB:** Supabase (Postgres + Auth)
- **اللغة:** عربية بالكامل، **RTL** إجباري من البداية

---

## متطلبات RTL (إجبارية، طبقها من أول commit)

1. `<html lang="ar" dir="rtl">` في الجذر
2. استخدم **الخصائص المنطقية فقط** فـ Tailwind: `ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-` — **ممنوع** استخدام `ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-` فأي مكان
3. لف التطبيق بـ `<DirectionProvider dir="rtl">` من `@radix-ui/react-direction`
4. القائمة الجانبية (Sidebar) على **اليمين**
5. الأيقونات المتجهة (سهم رجوع، chevron) تتبدل حسب الاتجاه
6. تحقق من كل مكون تسحبه من shadcn/ui يشتغل صح فـ RTL قبل ما تعتمده

---

## الهوية البصرية (Design Tokens) - إجبارية

### الألوان
```css
:root {
  --teal-primary: #0F766E;
  --bg-light: #F7F9F8;
  --status-good: #2E9E6C;
  --status-pending: #C88A2D;
  --text-primary-light: #122420;
  --border-subtle-light: #DCE6E2;
}
.dark {
  --bg-dark: #0E1B18;
  --text-primary-dark: #E8F2EF;
  --border-subtle-dark: #1E3530;
}
```
- بلا `box-shadow` زخرفي على البطاقات — استخدم `border` رفيع فقط
- الألوان الوظيفية (أخضر/كهرماني) تُستخدم **فقط** كمؤشرات حالة حقيقية، ماشي ديكور
- بلا تدرجات (gradients) زخرفية

### التايبوغرافي
- النصوص العربية: **IBM Plex Sans Arabic** (استوردها من Google Fonts)
- الأرقام والأكواد (NIF/NIS/RC): **IBM Plex Mono** — استخدمها فأي مكان يظهر فيه رقم تعريفي، باش تتراصف بشكل مثالي فالجداول

### مبدأ التصميم
الجدول هو العنصر المركزي، ماشي البطاقات (cards). المحاسب يبغي يمسح بياناته بسرعة. نقطة حالة صغيرة ملونة (●) بدل badges صاخبة.

---

## مخطط قاعدة البيانات (Supabase)

```sql
create table clients (
  id uuid primary key default gen_random_uuid(),
  owner_name text not null,
  business_name text not null,
  nif text not null,
  nis text not null,
  rc text not null,
  activity_type text not null,
  location text not null,
  telegram_chat_id bigint unique,
  status text default 'active', -- active | inactive
  documents_status text default 'pending', -- 'up_to_date' | 'pending'
  created_at timestamp default now()
);
```

### قواعد التحقق (Validation) على مستوى الفورم
| الحقل | القاعدة |
|---|---|
| `nif` | أرقام فقط، الطول = 15 أو 20 بالضبط |
| `nis` | أرقام فقط، الطول = 15 أو 18 بالضبط |
| `rc` | نمط تقريبي: `\d{2}/\d{2}-\d{7}[A-Z]\d{2}` (مرن، لأن كاين اختلافات قديمة فالصيغة) |
| باقي الحقول | غير فارغة (required) |

---

## الأمان (إجباري، لا تتهاون)

1. الفرونت اند يستخدم **anon key** فقط من Supabase (أبداً service_role key)
2. كل طلبات القراءة/الكتابة تمر عبر **Supabase Auth session** + **Row Level Security (RLS)**
3. المفاتيح الحساسة فـ ملف `.env` غير مرفوع لـ git (`.gitignore`)
4. تحقق من صحة المدخلات (validation) على مستوى الفرونت اند **و** على مستوى قاعدة البيانات (constraints/RLS policies)، ماشي ثقة عمياء فمدخلات المستخدم

---

## المطلوب: صفحتين بالضبط

### الصفحة 1: الرئيسية (Dashboard Home)

**المحتوى:**
1. شريط إحصائيات علوي (3-4 بطاقات بسيطة، بلا ديكور زايد):
   - عدد التجار الكلي
   - عدد التجار "محدّثين" (documents_status = up_to_date)
   - عدد التجار "متأخرين" (documents_status = pending)
2. جدول التجار (Data Table من TanStack Table):
   - الأعمدة: نقطة الحالة (●) | الاسم واللقب | اسم المؤسسة | NIF | نوع النشاط | تاريخ الإضافة
   - بحث/فلترة فورية (بالاسم أو NIF)
   - فرز حسب أي عمود
   - Pagination إذا العدد كبير
3. زر بارز "إضافة تاجر جديد" يوجه لصفحة الفورم

**مصدر البيانات:** query مباشر لجدول `clients` عبر `supabase-js`، بلا mock data.

### الصفحة 2: إضافة تاجر (Add Trader Form)

**الحقول بالترتيب:**
1. الاسم واللقب (نص)
2. اسم المؤسسة أو الشركة (نص)
3. رقم NIF (نص، تحقق طول 15/20)
4. رقم NIS (نص، تحقق طول 15/18)
5. رقم السجل التجاري RC (نص، تحقق نمط)
6. نوع النشاط (نص أو select بسيط)
7. موقع النشاط (نص)

**السلوك:**
- تحقق فوري (inline validation) أثناء الكتابة، رسائل خطأ واضحة بالعربية أسفل كل حقل
- زر "حفظ" معطل حتى تكتمل كل الحقول بشكل صحيح
- عند النجاح: إدراج فـ Supabase + رسالة تأكيد + إعادة توجيه لصفحة الرئيسية
- عند الفشل (مثلاً NIF مكرر): رسالة خطأ واضحة تشرح المشكلة بالضبط، بلا رسائل تقنية غامضة

---

## معايير الجودة المطلوبة

- **TypeScript صارم:** عرّف `type Client { ... }` وستخدمو فكل مكان (الفورم، الجدول، الاستعلامات)
- **مكونات معزولة وقابلة لإعادة الاستخدام:** الفورم، الجدول، بطاقة الإحصائية = مكونات منفصلة
- **Responsive:** يشتغل صح على شاشة صغيرة (موبايل) وكبيرة
- **Accessibility:** focus واضح بالكيبورد، labels صحيحة على كل input
- **بلا نصوص إنجليزية** فالواجهة (الكود والمتغيرات بالإنجليزية، لكن كل نص يظهر للمستخدم بالعربية)

---

## خطوات العمل المتوقعة منك (الوكيل)

1. استنسخ shadcn-admin وافحص بنيته
2. جهز مشروع جديد نظيف بنفس الستاك التقني (Vite+TS+Tailwind+shadcn/ui+TanStack Router/Table)
3. طبق RTL كاملاً على مستوى الجذر قبل أي صفحة
4. طبق الهوية البصرية (الألوان + الخطوط) كـ design tokens فـ `tailwind.config` و`globals.css`
5. جهز اتصال Supabase (`supabase-js` client + `.env`)
6. ابني مكون "Client Data Table" (الصفحة الرئيسية)
7. ابني مكون "Add Client Form" مع كل قواعد التحقق
8. اختبر: أضف تاجر تجريبي، تأكد يظهر فالجدول فوراً
9. لخص لي فالنهاية: شنو بنيت بالضبط، وين لقيت صعوبات (إن وجدت)، وأي قرار تقني أخذته بنفسك وما كانش مذكور فهذا البرومبت

---

## ملاحظة أخيرة مهمة

لا تخترع صفحات أو ميزات زايدة غير مطلوبة فهذا البرومبت (بلا صفحة إعدادات، بلا صفحة تسجيل دخول الآن، بلا صفحة وثائق الآن). المطلوب حصرياً هو الصفحتين المذكورتين أعلاه، بجودة عالية واحترافية، لا أكثر ولا أقل.
